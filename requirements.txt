# Pinned to the versions verified live against the Hackathon subscription on 2026-06-13
# (Python 3.14). The knowledge base + agentic retrieval work on azure-search-documents 12.0.0.
azure-search-documents==12.0.0   # Foundry IQ: knowledge base / source + agentic retrieve (verified)
azure-identity==1.25.3           # DefaultAzureCredential (managed identity / az login)
azure-keyvault-secrets==4.11.0   # Anthropic key from Key Vault at runtime (FEAT-06.2)
azure-storage-blob==12.30.0      # upload public docs to the knowledge source container
anthropic==0.109.1               # Claude — the four agents' reasoning
openai==2.41.1                   # Azure OpenAI — Foundry IQ query planner (gpt-4.1-mini)
azure-ai-projects==2.2.0         # Foundry project / Agent Service (EPIC-02, Option A)
pydantic==2.13.4                 # output-schema validation

fastapi==0.115.6                 # thin HTTP wrapper around agent.run (FEAT-06.3, src/server.py)
uvicorn[standard]==0.34.0        # ASGI server for the container entrypoint

pytest==9.1.0                    # test runner for tests/ (TESTPLAN-01.3.x onward)
