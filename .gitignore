# Secrets & local config
.env
.env.*
!.env.example
*.key
*.pem

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/

# Local artefacts / outputs
/out/
/tmp/
*.local.json
runs/

# Tandem kit runtime (per-session mode tracker — never tracked)
.tandem-mode.json

# OS / editor
.DS_Store
.vscode/
.idea/

# Internal design / working notes — local reference only, NEVER publish (clean-room)
docs/design/

# Internal project management + Cowork planning — holds real Azure resource names/endpoints; NEVER publish
_00-Project-Management/
# Cowork planning scratch — explicitly ignored (local-only reference, never published)
_00-Project-Management/20-Requirements/cowork/
